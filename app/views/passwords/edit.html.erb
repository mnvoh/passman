<div class="container">
  <div class="row">
    <div class="col">
      <h1><%= t('edit_password') %></h1>

      <% if notice %>
        <p id="notice" class="alert alert-danger"><%= notice %></p>
      <% end %>

      <%= render 'form', password: @password %>
    </div>
  </div>
</div>
