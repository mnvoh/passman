<div class='wrapper'>
  <div class='header'>
    <%= icon 'fas', 'user-plus fa-5x', class: 'header-icon' %>
    <h2>Sign up</h2>
  </div>

  <div class='content'>
    <%= form_for(resource, as: resource_name, url: users_sign_up_path) do |f| %>
      <%= devise_error_messages! %>

      <div class="form-group">
        <%= icon 'fas', 'envelope fa-2x' %>
        <%=
          f.email_field :email, autofocus: true, autocomplete: "email",
            placeholder: 'Email Address'
        %>
      </div>

      <div class="form-group">
        <%= icon 'fas', 'key fa-2x' %>
        <%=
          f.password_field :password, autocomplete: "new-password",
            placeholder: 'Password'
        %>
      </div>

      <div class="form-group">
        <%= icon 'fas', 'key fa-2x' %>
        <%=
          f.password_field :password_confirmation, autocomplete: "new-password",
            placeholder: 'Repeat Password'
        %>
      </div>
      <% if @minimum_password_length %>
        <div class="text-left">
          <em class='text-muted text-left'><%= @minimum_password_length %> characters minimum</em>
        </div>
      <% end %><br />

      <div class="actions">
        <%= f.submit "Sign up", class: 'btn btn-primary' %>
      </div>
    <% end %>

    <%= render "devise/shared/links" %>
  </div>
</div>
